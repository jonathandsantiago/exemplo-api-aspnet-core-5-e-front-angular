version: '3.6'

services:
  favodemel-web:
    image: favodemel-web
    container_name: avaliacao-favodemel-web
    build: .
    ports:
      - 54200:80
    networks:
      - net

networks:
  net:
    driver: bridge
